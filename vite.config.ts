import { defineConfig } from 'vite'

export default defineConfig({
  // This tells Vite where your assets live relative to the domain
  base: '/vite-project2/', 
  
  // If you are using React or Vue, you'd add the plugins here
  plugins: [], 
})
